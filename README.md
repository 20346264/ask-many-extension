<p align="center">
  <img src="icons/icon128.png" width="96" height="96" alt="同问 AI 图标">
</p>

<h1 align="center">同问 AI · 多模型工作台</h1>

<p align="center">在一个页面中并排使用多个 AI，一次输入，同时提问。</p>

## 功能

- 在同一面板中并排打开 ChatGPT、DeepSeek、Claude 等模型
- 一次填写问题，并行填入并发送到已选择的模型
- 支持向已适配的各大主流模型同步分发图片或文档附件
- 等回答稳定后收集结果，生成结构化对比提示词
- **👑 对比裁判模型（自动评测）**：可指定任意一个已选模型（如 Claude / ChatGPT / DeepSeek）作为对比裁判。裁判模型不参与第一轮同问，在其他模型完成回答后，系统自动整理结构化对比提示词并自动派发给裁判模型，在裁判窗口中直接生成横向对比报告
- 保存模型选择状态与窗口显示顺序，下次打开继续使用
- 支持自定义模型窗口排序：左侧栏拖拽排序或窗口顶栏 ◀ / ▶ 按钮微调，无刷新即时生效
- **提供 4 种智能展示布局**：自适应多行网格（保底宽度 ≥ 360px 不受挤压）、横向滑动画廊、2列深度对比、3列深度对比，彻底解决大量模型勾选后的视口挤压问题
- **单模型全屏聚焦与快捷折叠**：支持单模型一键全屏（⛶）深读作答与代码，或折叠（−）为紧凑侧条腾出视野
- **左侧边栏一键收起/展开 (Ctrl+B)**：支持点击侧边栏右上角折叠按钮（≪）或主界面顶栏开关一键收起，释放 100% 水平屏幕空间给多模型并排阅读，支持 `Ctrl+B` 快捷键与状态记忆
- **侧边栏批量选择与常用预设**：支持一键【全选】、【清空】以及【国内精选】/【海外主流】预设组合
- 直接使用浏览器中的网站登录状态，不需要 API Key

## 支持站点

| 站点 | 文字发送 | 图片/文件 | 说明 |
|---|:---:|:---:|---|
| ChatGPT | 是 | 是 | OpenAI 官方网页版 |
| DeepSeek | 是 | 是 | 深度求索官方网页版 |
| Claude | 是 | 是 | Anthropic Claude 网页版 |
| Gemini | 是 | 是 | Google Gemini 网页版 |
| 通义千问 | 是 | 是 | 阿里通义千问网页版（支持多模态附件） |
| 腾讯元宝 | 是 | 是 | 腾讯元宝网页版（Quill 编辑器 + 附件注入） |
| Kimi | 是 | 是 | 月之暗面 Kimi（超长文本与文献解析） |
| 豆包 | 是 | 是 | 字节跳动豆包 AI 对话 |
| 智谱清言 | 是 | 是 | 智谱清言 GLM-4 网页端 |
| Grok | 是 | 是 | xAI Grok 对话平台 |
| Perplexity | 是 | 是 | 全球领先的 AI 搜索研究平台 |
| 小米 MiMo | 是 | 是 | 小米官方自研大模型（AI Studio 对话与多模态） |

站点页面更新后，输入框或发送按钮的选择器可能需要同步调整。

## 安装

1. 点击 GitHub 页面右上角的 **Code → Download ZIP**。
2. 解压 ZIP，并把目录放到长期保留的位置。
3. 在 Chrome 地址栏打开 **chrome://extensions/**。
4. 打开右上角的“开发者模式”。
5. 点击“加载已解压的扩展程序”。
6. 选择包含 **manifest.json** 的目录。
7. 固定工具栏中的“同问 AI”图标，然后点击图标打开工作台。

更新代码后，在 **chrome://extensions/** 中点击扩展卡片上的“重新加载”，并重新打开工作台。

## 使用

1. 在左侧勾选参与提问的模型，并在各模型页面完成登录。
2. （可选）**设置对比裁判**：在底部状态栏的【👑 对比裁判】下拉菜单或各模型顶栏的【👑】按钮中指定裁判模型。启用【自动评测】后，该模型将作为终审裁判，不参与第一轮直接回答。
3. 在底部输入问题；需要时添加图片或文件。
4. 按 **Enter** 或点击“发送”：系统将问题并行派发给所有参与作答的模型。
5. 作答完成后，系统将自动汇总各家回答生成深度对比提示词（涵盖核心共识、关键分歧表格、各家亮点、事实风险检验与权威裁决），并自动派发给裁判模型；亦可点击“收集对比”手动查看与一键派发。

**Ctrl+Enter** 和 **Shift+Enter** 用于换行。中文输入法选字时不会触发发送。

## 权限与安全

多数 AI 网站默认禁止被其他页面嵌入。为了实现单页并排显示，扩展会在工作台打开期间移除目标 iframe 响应中的 **X-Frame-Options** 和相关 CSP 响应头。

这条规则具有以下限制：

- 只在当前工作台标签页中生效
- 只作用于子框架请求
- 只覆盖 manifest.json 中声明的 AI 站点
- 工作台关闭或导航离开后自动撤销

扩展没有自己的远程服务器，不读取或保存 AI 网站的账号密码。登录状态仍由 Chrome 和各 AI 网站管理。

## 已知限制

- 首次使用需要分别登录各 AI 网站
- 人机验证、登录过期或站点新增 iframe 限制时，对应模型可能无法嵌入
- AI 网站更新页面结构后，自动填入、发送或收集可能暂时失效
- 当前通过开发者模式安装，尚未发布到 Chrome Web Store

## 目录

- **adapters.js**：站点选择器和 DOM 操作
- **background.js**：面板打开与 iframe 规则
- **content.js**：iframe 内消息处理
- **panel.html**：工作台界面
- **panel.js**：多模型编排
- **manifest.json**：Chrome 扩展清单
- **icons/**：多尺寸扩展图标

## 许可证

[MIT](LICENSE)
